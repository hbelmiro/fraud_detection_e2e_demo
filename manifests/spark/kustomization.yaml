apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - spark-sa.yaml
  - spark-role.yaml
  - spark-role-binding.yaml
  - spark-sa.yaml

commonLabels:
  app: spark
  component: processing